import { ConvertFToCPipe } from './convert-f-to-c.pipe';

describe('ConvertFToCPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertFToCPipe();
    expect(pipe).toBeTruthy();
  });
});
